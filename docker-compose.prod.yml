version: '3.8'
services:
 nodejs:
  container_name: express
  build: 
   context: .
   dockerfile: Dockerfile 
  ports: 
   - 4000:4000
  environment:
    - NODE_MODULE=PROD
  volumes:
   - ./src/:/APP/src/ 
  env_file:
   - ./.env 